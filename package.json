{
  "name": "example-app",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Building the app' && mkdir -p dist",
    "test": "echo 'Running tests'",
    "start": "echo 'Starting the app'"
  }
}
